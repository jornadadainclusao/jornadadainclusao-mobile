<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Integrakidsmobile">

        <!-- AGORA ESTA É A PRINCIPAL -->
        <activity
            android:name=".ConexaoActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity
            android:name=".profile.PerfilTrocarPlayer"
            android:exported="false" />

        <activity
            android:name=".profile.PerfilResultados"
            android:exported="false" />

        <activity
            android:name=".profile.PerfilEditKid"
            android:exported="false" />

        <activity
            android:name=".profile.PerfilCadKid"
            android:exported="false" />

        <activity
            android:name=".profile.PerfilEditCad"
            android:exported="false" />

        <activity
            android:name=".games.views.jogo_numeros.JogoNumeros"
            android:exported="false" />

        <activity
            android:name=".games.views.jogo_vogais.JogoVogais"
            android:exported="false" />

        <activity
            android:name=".games.views.jogo_memoria.JogoMemoria"
            android:exported="false" />

        <activity
            android:name=".games.views.jogo_cores.JogoCores"
            android:exported="false" />

        <activity
            android:name=".MenuPrincipal"
            android:exported="false"
            android:label="@string/title_activity_menu_principal" />

        <activity
            android:name=".LoginCadastro"
            android:exported="false" />

    </application>

</manifest>
