<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Cor de fundo -->
    <solid android:color="#00FFFFFF" />

    <!-- Borda -->
    <stroke
        android:width="2dp"
        android:color="@color/icon" />

    <!-- Cantos arredondados -->
    <corners android:radius="12dp" />

    <!-- Padding interno (opcional, se não quiser usar padding no EditText) -->
    <padding
        android:left="12dp"
        android:top="12dp"
        android:right="12dp"
        android:bottom="12dp"/>
</shape>
